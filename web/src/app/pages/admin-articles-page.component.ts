import { Component, inject } from '@angular/core'; import { CommonModule } from '@angular/common'; import { FormsModule } from '@angular/forms'; import { ArticleRepository } from '../repositories/article.repository'; import { Article, ArticleDraft } from '../models/article.models'; import { MarkdownService } from '../services/markdown.service'; import { StatusMessageService } from '../shared/status-message.service';
@Component({ selector:'app-admin-articles-page', standalone:true, imports:[CommonModule,FormsModule], templateUrl:'./admin-articles-page.component.html', styleUrl:'./admin-articles-page.component.css' })
export class AdminArticlesPageComponent { private readonly repo=inject(ArticleRepository); private readonly status=inject(StatusMessageService); private readonly md=inject(MarkdownService); protected items:Article[]=[]; protected editing:string|null=null; protected busy=false; protected preview=false; protected form:ArticleDraft=this.blank(); constructor(){void this.load();} private blank():ArticleDraft{return{title:'',slug:'',excerpt:'',content:'',coverUrl:'',category:'ความรู้',tags:[],status:'draft',author:'admin'};} private async load(){this.items=await this.repo.all(true);} protected start(item?:Article){this.editing=item?.id??null;this.form=item?{...item,tags:[...(item.tags??[])]}:this.blank();this.preview=false;} protected slugify(){this.form.slug=this.form.slug||this.form.title.toLowerCase().trim().replace(/[^a-z0-9ก-๙]+/gi,'-').replace(/^-|-$/g,'');} protected rendered(){return this.md.render(this.form.content);} protected async save(status: 'draft'|'published'=this.form.status){if(this.busy)return;this.form.status=status;if(!this.form.title.trim()||!this.form.content.trim()||!this.form.slug.trim()){this.status.show('กรุณากรอกชื่อเรื่อง, slug และเนื้อหา','error');return;}this.busy=true;this.status.show(status==='published'?'กำลังเผยแพร่บทความ…':'กำลังบันทึกบทความ…');try{await this.repo.save({...this.form,tags:this.form.tags},this.editing??undefined);this.status.show(status==='published'?'เผยแพร่บทความสำเร็จ':'บันทึกบทความสำเร็จ','success');this.editing=null;this.form=this.blank();await this.load();}catch(e){this.status.show(e instanceof Error?e.message:'ไม่สามารถบันทึกบทความได้','error');}finally{this.busy=false;}} protected async remove(item:Article){if(this.busy||!confirm(`ยืนยันการลบ ${item.title} หรือไม่?`))return;this.busy=true;this.status.show('กำลังลบบทความ…');try{await this.repo.delete(item.id);this.status.show('ลบบทความสำเร็จ','success');await this.load();}catch(e){this.status.show(e instanceof Error?e.message:'ไม่สามารถลบบทความได้','error');}finally{this.busy=false;}} }
